<%= react_component("HelloWorld", { 
    is_logged_in: !current_user.nil?,
    sign_in_route: new_user_session_path,   # /users/sign_in
    sign_out_route: destroy_user_session_path, #/users/sign_out
    current_user: {
        name: current_user&.name
    }
}) %>